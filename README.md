# watchtower-file-check
file integrity monitor. it hashes a directory of files and saves the hashes. if you run it again it tells you if any files got modified or deleted.
pretty basic, using sha256.

`python watchtower.py`
